<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:views="clr-namespace:Shellf.Views">

    <views:ColorHexToAccentBrushConverter x:Key="Converter.ColorAccent"/>

    <!-- ============ Palette: black & grey, no accent color, no rounded corners ============ -->
    <SolidColorBrush x:Key="Brush.Window"       Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="Brush.Sidebar"      Color="#FF141414"/>
    <SolidColorBrush x:Key="Brush.Panel"        Color="#FF1C1C1C"/>
    <SolidColorBrush x:Key="Brush.Border"       Color="#FF2A2A2A"/>
    <SolidColorBrush x:Key="Brush.BorderFocus"  Color="#FF6E6E6E"/>
    <SolidColorBrush x:Key="Brush.Text"         Color="#FFEDEDED"/>
    <SolidColorBrush x:Key="Brush.TextMuted"    Color="#FF7A7A7A"/>
    <SolidColorBrush x:Key="Brush.Accent"       Color="#FFFAFAFA"/>
    <SolidColorBrush x:Key="Brush.AccentHover"  Color="#FFD4D4D4"/>
    <SolidColorBrush x:Key="Brush.AccentText"   Color="#FF0E0E0E"/>
    <SolidColorBrush x:Key="Brush.Hover"        Color="#FF232323"/>
    <SolidColorBrush x:Key="Brush.Selected"     Color="#FF2A2A2A"/>
    <SolidColorBrush x:Key="Brush.TerminalBg"   Color="#FF0C0C0C"/>
    <SolidColorBrush x:Key="Brush.TerminalText" Color="#FFD0D0D0"/>
    <SolidColorBrush x:Key="Brush.CloseHover"   Color="#FFD45454"/>
    <SolidColorBrush x:Key="Brush.ScrollThumb"  Color="#FF3A3A3A"/>

    <!-- ============ Button ============ -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Hover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderFocus}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{StaticResource Brush.Accent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Accent}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.AccentText}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.AccentHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.AccentHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Title bar caption buttons (min/max/close), Segoe MDL2 glyphs -->
    <Style x:Key="CaptionButton" TargetType="Button">
        <Setter Property="Width" Value="46"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Hover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CaptionCloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#C42B1C"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Borderless icon button (sidebar toggle etc.) -->
    <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Hover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Tab close: Lucide "x" as a two-state icon — muted grey at rest,
         bright white (with a soft hover pad) under the pointer. -->
    <Style x:Key="TabCloseButton" TargetType="Button">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="Transparent" Padding="4">
                        <Path x:Name="Cross"
                              Data="M18 6 6 18 M6 6 18 18"
                              Stroke="{StaticResource Brush.TextMuted}"
                              StrokeThickness="2"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Stretch="Uniform" Width="9" Height="9"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Cross" Property="Stroke" Value="#FFFFFF"/>
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Hover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ TextBox ============ -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="CaretBrush" Value="{StaticResource Brush.Text}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Brush.Selected}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderFocus}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============ ListBox (sidebar groups) ============ -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="Transparent"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Selected}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ ScrollBar (flat, dark, square) ============ -->
    <Style x:Key="ScrollPageButton" TargetType="RepeatButton">
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Rectangle Fill="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Matches the terminal scrollbar: 8px, flush #3A3A3A thumb, lighter on hover -->
    <Style x:Key="ScrollThumb" TargetType="Thumb">
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBd" Background="{StaticResource Brush.ScrollThumb}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBd" Property="Background" Value="#4A4A55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <!-- MinWidth 0 is load-bearing: ScrollBar's default minimum is the system
             scrollbar metric (~17), which silently floors any slimmer Width. -->
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Width" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Style="{StaticResource ScrollPageButton}" Command="ScrollBar.PageUpCommand"/>
                        </Track.DecreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb Style="{StaticResource ScrollThumb}"/>
                        </Track.Thumb>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Style="{StaticResource ScrollPageButton}" Command="ScrollBar.PageDownCommand"/>
                        </Track.IncreaseRepeatButton>
                    </Track>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="Height" Value="5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollPageButton}" Command="ScrollBar.PageLeftCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollPageButton}" Command="ScrollBar.PageRightCommand"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Explicit handles: WPF's implicit-style lookup does not reliably reach
         template-internal ScrollBars, so templates below reference these by key. -->
    <Style x:Key="SlimScrollBar" TargetType="ScrollBar" BasedOn="{StaticResource {x:Type ScrollBar}}"/>

    <ControlTemplate x:Key="SlimScrollViewer" TargetType="ScrollViewer">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"
                                    Margin="{TemplateBinding Padding}"
                                    CanContentScroll="{TemplateBinding CanContentScroll}"/>
            <ScrollBar x:Name="PART_VerticalScrollBar"
                       Grid.Column="1" Grid.Row="0"
                       Style="{StaticResource SlimScrollBar}"
                       Orientation="Vertical"
                       Minimum="0"
                       Maximum="{TemplateBinding ScrollableHeight}"
                       ViewportSize="{TemplateBinding ViewportHeight}"
                       Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
            <ScrollBar x:Name="PART_HorizontalScrollBar"
                       Grid.Column="0" Grid.Row="1"
                       Style="{StaticResource SlimScrollBar}"
                       Orientation="Horizontal"
                       Minimum="0"
                       Maximum="{TemplateBinding ScrollableWidth}"
                       ViewportSize="{TemplateBinding ViewportWidth}"
                       Value="{Binding HorizontalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
        </Grid>
    </ControlTemplate>

    <!-- ============ TreeView (sidebar workspace tree) ============ -->
    <Style TargetType="TreeView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <ScrollViewer Focusable="False"
                                  CanContentScroll="False"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto"
                                  Template="{StaticResource SlimScrollViewer}">
                        <ItemsPresenter/>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Row background comes from the item (group colour tint or transparent). Hover
         and selection never replace it: they layer translucent white on top, so tinted
         rows brighten instead of reverting to grey. The expanded children area shows
         the same tint at reduced strength, so a whole open group reads as one block. -->
    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="2,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <!-- Transparent root: the 3px spacing seam above each row hit-tests to
                         THIS item, not the parent group — otherwise clicks landing a pixel
                         or two high select the group header instead of the tab. -->
                    <Grid Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <!-- The row (background + state highlight) always spans the full
                             pane width; indentation is applied to the CONTENT via Padding,
                             so nested rows highlight edge-to-edge like top-level ones. -->
                        <Border x:Name="Bd" Grid.Row="0" Background="{TemplateBinding Background}" Margin="0,3,0,0">
                            <Grid>
                                <!-- Collapsed groups double up their tint so the colour
                                     stays prominent when the block below is hidden. -->
                                <Rectangle x:Name="CollapsedBoost" Fill="{TemplateBinding Background}" Visibility="Collapsed"/>
                                <Rectangle x:Name="StateLayer" Fill="Transparent"/>
                                <!-- Selection: full-width fill with an accent border — the
                                     border colour comes from the owning group (falls back
                                     to the default grey for loose tabs). -->
                                <Rectangle x:Name="SelectionLayer"
                                           Fill="#1FFFFFFF"
                                           StrokeThickness="1"
                                           Visibility="Collapsed"
                                           Stroke="{Binding DataContext.ColorHex, RelativeSource={RelativeSource FindAncestor, AncestorType=TreeViewItem, AncestorLevel=2}, Converter={StaticResource Converter.ColorAccent}, FallbackValue={StaticResource Brush.BorderFocus}}"/>
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="18"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Expander"
                                                  Grid.Column="0"
                                                  ClickMode="Press"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Padding="4">
                                                    <Path x:Name="Arrow"
                                                          Data="M9 6 15 12 9 18"
                                                          Stroke="{StaticResource Brush.TextMuted}"
                                                          StrokeThickness="2"
                                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                                          Stretch="Uniform" Width="8" Height="10"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                                          RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="0"/>
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Arrow" Property="RenderTransform">
                                                            <Setter.Value>
                                                                <RotateTransform Angle="90"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter TargetName="Arrow" Property="Stroke" Value="{StaticResource Brush.Text}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Arrow" Property="Stroke" Value="{StaticResource Brush.Text}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center"/>
                                </Grid>
                            </Grid>
                        </Border>
                        <Grid x:Name="ItemsHost" Grid.Row="1">
                            <!-- Same brush at full strength as the header: an open group
                                 reads as one flat colored block, no darker sub-shade. -->
                            <Rectangle Fill="{TemplateBinding Background}"/>
                            <ItemsPresenter/>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <!-- Collapsed group: intensify the tint on the header -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsExpanded" Value="False"/>
                                <Condition Property="HasItems" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CollapsedBoost" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>
                        <Trigger SourceName="Bd" Property="IsMouseOver" Value="True">
                            <Setter TargetName="StateLayer" Property="Fill" Value="#17FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectionLayer" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ Context menus ============ -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="MinWidth" Value="190"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{StaticResource Brush.Panel}"
                            BorderBrush="{StaticResource Brush.Border}"
                            BorderThickness="1"
                            Padding="4">
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Submenu headers need their own template with the flyout Popup. -->
    <ControlTemplate x:Key="Template.MenuItem.SubmenuHeader" TargetType="MenuItem">
        <Grid>
            <Border x:Name="Bd" Background="Transparent" Padding="12,7">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="0" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="▸" Foreground="{StaticResource Brush.TextMuted}"
                               Margin="14,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            <Popup x:Name="PART_Popup"
                   Placement="Right"
                   HorizontalOffset="2"
                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                   AllowsTransparency="True"
                   Focusable="False">
                <Border Background="{StaticResource Brush.Panel}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1"
                        Padding="4"
                        MinWidth="170">
                    <ItemsPresenter/>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Hover}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Separators inside menus resolve this key, not the implicit Separator style. -->
    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Height="1" Margin="4,4" Fill="{StaticResource Brush.Border}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Bd" Background="Transparent" Padding="12,7">
                        <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource Template.MenuItem.SubmenuHeader}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============ ToolTip ============ -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
    </Style>

    <!-- ============ Icons ============
         Lucide-style stroke icons on a 24x24 grid, kept as vector drawings so they
         scale crisply and recolor with the theme. The transparent bounds rect pins
         every icon to the same 24x24 box so displayed sizes stay consistent. -->
    <Pen x:Key="Pen.Icon" Brush="{StaticResource Brush.Text}" Thickness="2"
         StartLineCap="Round" EndLineCap="Round" LineJoin="Round"/>
    <Geometry x:Key="Geometry.IconBounds">M0,0H24V24H0Z</Geometry>

    <!-- Raw folder geometry for places that recolor the icon per item (group accents) -->
    <Geometry x:Key="Geometry.Folder">M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z</Geometry>

    <DrawingImage x:Key="Icon.Plus">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}" Geometry="M12 5 12 19 M5 12 19 12"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="Icon.ChevronDown">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}" Geometry="M6 9 12 15 18 9"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- The cog: outer ring r=8, hub r=2, and 12 teeth (one every 30°, r=10 -> r=8) -->
    <DrawingImage x:Key="Icon.Settings">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <EllipseGeometry Center="12,12" RadiusX="8" RadiusY="8"/>
                            <EllipseGeometry Center="12,12" RadiusX="2" RadiusY="2"/>
                            <PathGeometry Figures="M12 2 12 4 M12 22 12 20 M2 12 4 12 M22 12 20 12
                                                   M20.66 17 18.93 16 M20.66 7 18.93 8
                                                   M3.34 17 5.07 16 M3.34 7 5.07 8
                                                   M17 20.66 16 18.93 M17 3.34 16 5.07
                                                   M7 20.66 8 18.93 M7 3.34 8 5.07"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="Icon.Folder">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}"
                                 Geometry="M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="Icon.Terminal">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}" Geometry="M4 17 10 11 4 5 M12 19 20 19"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "square-chevron-right": Command Prompt tabs -->
    <DrawingImage x:Key="Icon.Cmd">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="3,3,18,18" RadiusX="2" RadiusY="2"/>
                            <PathGeometry Figures="M10 8 14 12 10 16"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "panel-left": sidebar collapse/expand toggle -->
    <DrawingImage x:Key="Icon.PanelLeft">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="3,3,18,18" RadiusX="2" RadiusY="2"/>
                            <PathGeometry Figures="M9 3 9 21"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "sticky-note": terminal toolbar, notes panel toggle -->
    <DrawingImage x:Key="Icon.Note">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}"
                                 Geometry="M16 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h11l5-5V5a2 2 0 0 0-2-2Z M15 21v-4a2 2 0 0 1 2-2h4"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "copy": terminal toolbar, copy current path -->
    <DrawingImage x:Key="Icon.Copy">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="8,8,14,14" RadiusX="2" RadiusY="2"/>
                            <PathGeometry Figures="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "folder-open": terminal toolbar, reveal current path in Explorer -->
    <DrawingImage x:Key="Icon.FolderOpen">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}"
                                 Geometry="m6 14 1.5-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.54 6a2 2 0 0 1-1.95 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.9a2 2 0 0 1 1.69.9l.81 1.2a2 2 0 0 0 1.67.9H18a2 2 0 0 1 2 2v2"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "save": the Save Workspace button -->
    <DrawingImage x:Key="Icon.Save">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}"
                                 Geometry="M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7 M7 3v4a1 1 0 0 0 1 1h7"/>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "git-compare-arrows": Git Bash tabs -->
    <DrawingImage x:Key="Icon.GitBash">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <EllipseGeometry Center="5,6" RadiusX="3" RadiusY="3"/>
                            <EllipseGeometry Center="19,18" RadiusX="3" RadiusY="3"/>
                            <PathGeometry Figures="M12 6h5a2 2 0 0 1 2 2v7 M15 9 12 6 15 3 M12 18H7a2 2 0 0 1-2-2V9 M9 15 12 18 9 21"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!-- Lucide "square-terminal": WSL tabs (a contained environment) -->
    <DrawingImage x:Key="Icon.Wsl">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent" Geometry="{StaticResource Geometry.IconBounds}"/>
                <GeometryDrawing Pen="{StaticResource Pen.Icon}">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <RectangleGeometry Rect="3,3,18,18" RadiusX="2" RadiusY="2"/>
                            <PathGeometry Figures="M7 11 9 9 7 7 M11 13 15 13"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

</ResourceDictionary>
